diff --git a/DESIGN.md b/DESIGN.md
index d2a0212..0d120f4 100644
--- a/DESIGN.md
+++ b/DESIGN.md
@@ -16,7 +16,7 @@ New spacing tokens: `--space-1:4px`, `--space-2:8px`, `--space-3:12px`, `--space
 - Page header: eyebrow, single title, short description. No repeated H1 from Markdown.
 - Primary action: existing `.btn`, one prominent start/resume action per entry page. Hover, pressed and keyboard focus visible. Links navigate; buttons change state. On「我的学习」, the primary button opens the current task’s first required textbook section; parallel basics are secondary links.
 - Route list: the home page no longer presents three job tracks as the main choice. Remaining track pages are lookup groupings, not the progress spine.
-- Lesson list: the current task comes from `content/learning-plan.json`. Catalog and track lists may still use `content/tracks.json` or the full book. Read status is text as well as color and means only “read”, never “task complete”. Footer navigation retains route context when a lookup track is open. Catalog navigation explicitly follows the full book.
+- Lesson list: the current task comes from `content/learning-plan.json`. On「我的学习」, the reader can manually pick another week of the same spine; this only changes which exercise is shown and does not mark earlier weeks complete. Catalog and track lists may still use `content/tracks.json` or the full book. Read status is text as well as color and means only “read”, never “task complete”. Footer navigation retains route context when a lookup track is open. Catalog navigation explicitly follows the full book.
 - Reference disclosure: native `details/summary` using `.fold`; collapsed by default, keyboard operable, linked headings automatically open ancestor disclosures before scrolling.
 - Guided project: existing `.guided-lab`, `.guided-step`, `.gs-btn`, `.gl-bar` primitives. One step progress summary; native details at end contains final run/explanation self-checks. Preserve stored legacy read/lab/project/guided data. Opening a solution does not complete a step.
 - Secondary navigation:「我的学习」/「全部章节」, current page marked with `aria-current`. The six homework projects are not in primary navigation. `#/projects` remains a retired-mainline explanation page so old bookmarks do not 404. No empty home TOC.
diff --git a/README.md b/README.md
index c93edd0..f567a24 100644
--- a/README.md
+++ b/README.md
@@ -12,7 +12,7 @@
 
 ## 从哪里开始
 
-打开课程首页的 [我的学习](https://xhr0417.github.io/llm-course/#/)，当前任务是 **小模型学习实验室—Attention**。页面会给出要读的教材小节、在空文件里先写单头再写多头的步骤，以及如何检查结果。本站不代写核心算法。
+打开课程首页的 [我的学习](https://xhr0417.github.io/llm-course/#/)，默认练习是第 1 周 **小模型学习实验室—Attention**。同一页可以手动改到第 2–8 周：只更换练习说明，不表示前面已经完成。每一周都写清写什么、怎么检查、必需做到哪里。本站不代写核心算法。
 
 教材、交互演示和搜索仍可用：
 
@@ -30,7 +30,7 @@ cd llm-course
 python3 -m http.server 8000
 ```
 
-浏览器打开 <http://127.0.0.1:8000>。课程网站是静态站，不需要安装前端依赖。打开首页即可看到当前 Attention 任务。
+浏览器打开 <http://127.0.0.1:8000>。课程网站是静态站，不需要安装前端依赖。打开首页即可看到第 1 周 Attention；同一页可手动改到第 2–8 周。
 
 ## 仓库中仍保留的项目目录（可选参考，非当前作业）
 
diff --git a/content/learning-plan.json b/content/learning-plan.json
index 9656703..4deb843 100644
--- a/content/learning-plan.json
+++ b/content/learning-plan.json
@@ -45,6 +45,58 @@
     {
       "id": "python-index",
       "name": "函数、索引、调试"
+    },
+    {
+      "id": "residual-ffn",
+      "name": "残差与前馈层"
+    },
+    {
+      "id": "lm-forward",
+      "name": "最小语言模型前向"
+    },
+    {
+      "id": "autograd-step",
+      "name": "loss、反向与参数更新"
+    },
+    {
+      "id": "modern-block",
+      "name": "RMSNorm / RoPE / SwiGLU"
+    },
+    {
+      "id": "gqa",
+      "name": "分组查询注意力"
+    },
+    {
+      "id": "label-shift",
+      "name": "next-token 与 label shift"
+    },
+    {
+      "id": "overfit-loop",
+      "name": "训练/验证循环与小样本过拟合"
+    },
+    {
+      "id": "tokenizer-bpe",
+      "name": "Tokenizer 与 BPE"
+    },
+    {
+      "id": "checkpoint",
+      "name": "学习率、日志与 checkpoint"
+    },
+    {
+      "id": "sft-mask",
+      "name": "Chat template 与 response-only loss"
+    },
+    {
+      "id": "eval-scorer",
+      "name": "简化评测与错误分类"
+    },
+    {
+      "id": "one-variable",
+      "name": "单变量实验"
+    },
+    {
+      "id": "kv-cache",
+      "name": "KV Cache 生成对照"
     }
   ],
   "tasks": [
@@ -52,6 +104,7 @@
       "id": "lab.attention.causal-mha",
       "stageId": "lab",
       "weekBudget": 1,
+      "shortTitle": "Attention",
       "title": "手写因果多头注意力",
       "goal": "在空文件里不看参考实现写出因果多头注意力；能讲清每一步 shape；改变未来 token 不改变之前位置的输出。",
       "conceptIds": ["attention", "causal-mask", "tensor-shape", "python-index"],
@@ -206,6 +259,733 @@
           "text": "关闭 dropout，用固定输入分别检查单头和多头。对照必须与你的实现使用同一套因果掩码、缩放和 dtype。多头还要事先约定拆头顺序，以及拼头之后有没有输出投影。先确认没有 NaN 或 Inf，再对每个有限数值检查 abs(实际值 - 参考值) <= atol + rtol * abs(参考值)；这是一条组合容差，不是 atol 和 rtol 分开各过一次。7.12 是无因果掩码的手算，只保留三位小数，不能当作因果注意力的数值标准。本站不运行 Python，也不提供核心算法答案。"
         }
       ]
+    },
+    {
+      "id": "lab.decoder.min-lm",
+      "stageId": "lab",
+      "weekBudget": 2,
+      "shortTitle": "最小语言模型",
+      "title": "拼最小 decoder，跑通 [B, T, V] 前向",
+      "goal": "用简单固定结构写出最小 decoder-only 前向：embedding、一层残差块、FFN、输出头。输入 token 得到 logits [B, T, V]。本周不把 RMSNorm / RoPE 当硬门槛。",
+      "conceptIds": ["lm-forward", "residual-ffn", "tensor-shape", "attention"],
+      "workspace": {
+        "where": "在本机新建空文件，例如 ~/llm-lab/min_lm.py。不要写进本站 projects/ 下的六个旧作业目录。可以调用上周自己写的注意力，但本周要自己接线。",
+        "inputs": "整数 token 序列，形状 [B, T]；词表大小 V；模型宽度 D。先用固定的 nn.Embedding、一层 Pre-LN 或甚至先不用 Norm 的残差块、GELU/ReLU FFN、线性输出头。位置可以用学到的 position embedding，也可以先不加。",
+        "outputs": "logits，形状 [B, T, V]。不要求本周就接上采样循环。"
+      },
+      "materials": [
+        {
+          "role": "required",
+          "chapterId": "gpt",
+          "section": "9.1 GPT：Decoder-only Transformer ★",
+          "label": "9.1 GPT：Decoder-only Transformer"
+        },
+        {
+          "role": "required",
+          "chapterId": "gpt",
+          "section": "9.2 Next Token Prediction ★",
+          "label": "9.2 Next Token Prediction"
+        },
+        {
+          "role": "required",
+          "chapterId": "transformer",
+          "section": "7.19 残差连接（含 mask 泄露问题）★",
+          "label": "7.19 残差连接"
+        },
+        {
+          "role": "required",
+          "chapterId": "transformer",
+          "section": "7.21 FFN ★",
+          "label": "7.21 FFN"
+        },
+        {
+          "role": "required",
+          "chapterId": "transformer",
+          "section": "7.20 LayerNorm（为什么不用 BatchNorm）★",
+          "label": "7.20 LayerNorm（本周可用简单 LN，不必换成 RMSNorm）"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "build-llm-1",
+          "section": "Lab 6：组装 Transformer Block",
+          "label": "Lab 6 组装 Block（写完后再对照）"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "build-llm-1",
+          "section": "Lab 7：搭出完整的 Decoder-only LM",
+          "label": "Lab 7 最小 LM（写完后再对照）"
+        },
+        {
+          "role": "parallel",
+          "chapterId": "pytorch",
+          "section": "11.3 形状变换四件套（LLM 代码的重灾区）",
+          "label": "11.3 形状变换"
+        },
+        {
+          "role": "parallel",
+          "chapterId": "pytorch",
+          "section": "11.6 nn.Module 与 Parameter：组织模型",
+          "label": "11.6 nn.Module"
+        },
+        {
+          "role": "reference",
+          "chapterId": "modern-llm",
+          "section": "10.3 RoPE：旋转位置编码 ★",
+          "label": "10.3 RoPE（可开始读，不是本周硬门槛）"
+        },
+        {
+          "role": "reference",
+          "chapterId": "modern-llm",
+          "section": "10.4 RMSNorm：更简单的归一化 ★",
+          "label": "10.4 RMSNorm（可开始读，不是本周硬门槛）"
+        }
+      ],
+      "steps": [
+        {
+          "title": "先画数据流再写",
+          "body": "在纸上或注释里写出：[B, T] → embedding [B, T, D] → 一层 block → lm_head → [B, T, V]。先写输入输出和每一步 shape，再动手。注意力用上周通过的实现或等价的单层因果注意力。"
+        },
+        {
+          "title": "实现固定结构的一层 block",
+          "body": "最小块：残差 + FFN（ReLU 或 GELU 即可）。LayerNorm 可用、也可先做成「先加残差再输出」。不要本周同时改 RMSNorm、RoPE、SwiGLU。一次只接一条通路。"
+        },
+        {
+          "title": "接上 embedding 和输出头",
+          "body": "用 nn.Embedding 和线性层得到 logits。随机初始化即可。前向传入一小批整数 token，打印每一步 shape，确认最后是 [B, T, V]。"
+        },
+        {
+          "title": "检查结果",
+          "body": "用固定 B=2、T=4、V 和 D 能整除的小数字跑一次。确认无 NaN/Inf；改最后一个 token 不要求本周再验因果（因果已在上周验），但残差前后 shape 必须一致。能说明残差加的是什么、输出头为什么是 V 维。本站不运行 Python。"
+        },
+        {
+          "title": "卡住时可以分层求助",
+          "body": "先留下一次接线尝试。卡住时请人解释 shape 报错或残差该加在哪；仍卡住再看 Lab 6 / Lab 7 局部。合上参考后重写 block 的 forward，并换一组 B、T 再跑。"
+        },
+        {
+          "title": "用一个小变式检查能否迁移",
+          "body": "任选一项：把 T 换成另一个长度，先预测 shape 再运行；或者把 FFN 隐藏维改成 4D，确认只有中间层变宽。这不是额外门槛。"
+        }
+      ],
+      "criteria": [
+        {
+          "id": "forward-btv",
+          "label": "前向形状",
+          "required": true,
+          "conceptId": "lm-forward",
+          "text": "输入 token [B, T]，输出 logits [B, T, V]。能指出 embedding、block、输出头各自的形状。"
+        },
+        {
+          "id": "residual-head",
+          "label": "残差与输出头",
+          "required": true,
+          "conceptId": "residual-ffn",
+          "text": "能说明残差加的是哪一路、输出头为什么映射到词表。与一份简单参考对照过 shape，不是只抄完整现代骨架。"
+        },
+        {
+          "id": "norm-rope-read",
+          "label": "归一化与位置",
+          "required": false,
+          "conceptId": "modern-block",
+          "text": "拓展：能解释本周没用 RMSNorm / RoPE 时，位置和尺度信息缺了什么。未完成不阻挡本周核心任务。"
+        }
+      ]
+    },
+    {
+      "id": "lab.train.step-update",
+      "stageId": "lab",
+      "weekBudget": 3,
+      "shortTitle": "一次参数更新",
+      "title": "先打通 loss 和参数更新，再逐个换现代组件",
+      "goal": "在最小模型上算出交叉熵、backward、optimizer.step，确认参数真的变了。然后再逐个替换 RMSNorm / RoPE / SwiGLU。GQA 是拓展，不挡完成。",
+      "conceptIds": ["autograd-step", "modern-block", "gqa", "tensor-shape"],
+      "workspace": {
+        "where": "例如 ~/llm-lab/train_step.py，接上周的最小 LM。不要写进六个旧作业目录。",
+        "inputs": "随机小 batch：input_ids [B, T]，labels 先用同一序列做 next-token（本周重点是更新是否发生，label shift 的细节放到下周写清）。学习率用很小的常数。",
+        "outputs": "标量 loss；step 前后同一参数张量的数值有可见差异；无 NaN/Inf。"
+      },
+      "materials": [
+        {
+          "role": "required",
+          "chapterId": "pytorch",
+          "section": "11.7 autograd：把第 1 章的手算交给框架",
+          "label": "11.7 autograd"
+        },
+        {
+          "role": "required",
+          "chapterId": "pytorch",
+          "section": "11.9 训练循环：标准骨架",
+          "label": "11.9 训练循环骨架"
+        },
+        {
+          "role": "required",
+          "chapterId": "basics",
+          "section": "1.8 交叉熵 ★",
+          "label": "1.8 交叉熵"
+        },
+        {
+          "role": "required",
+          "chapterId": "basics",
+          "section": "1.6 前向传播与反向传播 ★",
+          "label": "1.6 反向传播"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "modern-llm",
+          "section": "10.4 RMSNorm：更简单的归一化 ★",
+          "label": "10.4 RMSNorm（更新打通后再换）"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "modern-llm",
+          "section": "10.3 RoPE：旋转位置编码 ★",
+          "label": "10.3 RoPE（一次只换这一处）"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "modern-llm",
+          "section": "10.6 SwiGLU：现代 FFN ★",
+          "label": "10.6 SwiGLU（一次只换这一处）"
+        },
+        {
+          "role": "reference",
+          "chapterId": "modern-llm",
+          "section": "10.2 MHA → MQA → GQA ★",
+          "label": "10.2 GQA（拓展，不挡完成）"
+        },
+        {
+          "role": "parallel",
+          "chapterId": "pytorch",
+          "section": "11.13 调试清单：shape 断言与最小复现",
+          "label": "11.13 调试清单"
+        }
+      ],
+      "steps": [
+        {
+          "title": "先做一次完整更新",
+          "body": "用上周最小模型。写 loss = cross_entropy(...)，loss.backward()，optimizer.step()。打印 step 前后某一层 weight 的均值或一个元素。确认 requires_grad 的参数参与了更新。先不要改结构。"
+        },
+        {
+          "title": "检查梯度不是摆设",
+          "body": "关掉更新再 backward 一次，看 grad 是否存在且有限。学习率改成 0 时参数应几乎不变。出现 NaN 先减小学习率或检查 logits 是否爆炸，不要同时换三个模块。"
+        },
+        {
+          "title": "再逐个替换现代组件",
+          "body": "每次只换一件：先 RMSNorm，跑通更新；再 RoPE；再 SwiGLU。每换一次都重复「loss 下降或至少参数在动」。不要一次改完再调试。"
+        },
+        {
+          "title": "卡住时可以分层求助",
+          "body": "先留下一次 backward 尝试。优先问：哪一个张量没有 grad、loss 为什么不接计算图。仍卡住再看 11.9 骨架或 Lab 里对应模块。合上后重写 step 函数，换一组随机输入再跑。"
+        },
+        {
+          "title": "用一个小变式检查能否迁移",
+          "body": "把学习率乘 10 或除 10，描述参数变化变大还是变小。这不是额外门槛。"
+        }
+      ],
+      "criteria": [
+        {
+          "id": "loss-backward",
+          "label": "loss 与 backward",
+          "required": true,
+          "conceptId": "autograd-step",
+          "text": "小输入上能算出有限 loss，backward 后参数有梯度。"
+        },
+        {
+          "id": "params-move",
+          "label": "参数确实更新",
+          "required": true,
+          "conceptId": "autograd-step",
+          "text": "optimizer.step 之后至少一层权重与 step 前不同。学习率为 0 时不再发生同样变化。"
+        },
+        {
+          "id": "gqa-heads",
+          "label": "GQA 头数关系",
+          "required": false,
+          "conceptId": "gqa",
+          "text": "拓展：能解释 GQA 里查询头与 KV 头的分组关系。延期可先读 10.2 对照，未完成不能阻挡本周核心任务。"
+        }
+      ]
+    },
+    {
+      "id": "lab.train.overfit-loop",
+      "stageId": "lab",
+      "weekBudget": 4,
+      "shortTitle": "过拟合小样本",
+      "title": "写出训练/验证循环，过拟合很小的数据",
+      "goal": "做出最小 next-token 训练循环：训练集 loss 能降到很小，并留一份不参与训练的验证样本。写清 label shift 只做一次。",
+      "conceptIds": ["overfit-loop", "label-shift", "lm-forward"],
+      "workspace": {
+        "where": "例如 ~/llm-lab/overfit.py。数据用自己写的几条短文本或整数序列，不要依赖六个旧作业的数据目录。",
+        "inputs": "很小的训练集（几条到几十条序列）+ 独立验证样本；DataLoader 或手动 batch 均可。",
+        "outputs": "训练曲线（至少记下若干 step 的 train loss）；验证样本上的 loss；一份说明 label 如何与输入对齐。"
+      },
+      "materials": [
+        {
+          "role": "required",
+          "chapterId": "gpt",
+          "section": "9.2 Next Token Prediction ★",
+          "label": "9.2 Next Token Prediction"
+        },
+        {
+          "role": "required",
+          "chapterId": "gpt",
+          "section": "9.3 训练时为什么能并行 ★",
+          "label": "9.3 训练时并行"
+        },
+        {
+          "role": "required",
+          "chapterId": "pytorch",
+          "section": "11.8 Dataset / DataLoader：喂数据",
+          "label": "11.8 Dataset / DataLoader"
+        },
+        {
+          "role": "required",
+          "chapterId": "pytorch",
+          "section": "11.9 训练循环：标准骨架",
+          "label": "11.9 训练循环"
+        },
+        {
+          "role": "required",
+          "chapterId": "evaluation",
+          "section": "3.5 欠拟合与过拟合",
+          "label": "3.5 欠拟合与过拟合"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "build-llm-2",
+          "section": "Lab 8：准备数据集",
+          "label": "Lab 8 数据集（写完后再对照）"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "build-llm-2",
+          "section": "必须理解：next-token 对齐 + Shift",
+          "label": "next-token 对齐与 Shift（写完后核对）"
+        },
+        {
+          "role": "parallel",
+          "chapterId": "evaluation",
+          "section": "3.2 数据划分",
+          "label": "3.2 数据划分"
+        }
+      ],
+      "steps": [
+        {
+          "title": "先写清 label 怎么对齐",
+          "body": "输入 x[:, :-1] 预测 y[:, 1:]，或等价地只 shift 一次。在注释里写明不要 shift 两次。用 3 个 token 的手写例子核对。"
+        },
+        {
+          "title": "接上循环和验证样本",
+          "body": "写 train loop：zero_grad → forward → loss → backward → step。每 N step 在留出的验证样本上算 loss。验证样本不得进入训练。"
+        },
+        {
+          "title": "过拟合很小的数据",
+          "body": "把模型容量相对数据调大或把数据极小，确认 train loss 明显下降。若完全不降，先查 label 对齐和 learning rate，不要加 DDP 或混合精度。"
+        },
+        {
+          "title": "卡住时可以分层求助",
+          "body": "先留下一次循环尝试。优先问 loss 是否在对 PAD 或错误位置监督。仍卡住再看 Lab 8 或 Shift 小节。合上后重写 shift 与一步 train，换一条序列再跑。"
+        },
+        {
+          "title": "用一个小变式检查能否迁移",
+          "body": "把验证样本换成另一条从未训练的序列，看 val loss 是否高于近乎为零的 train loss。能解释这为什么合理。"
+        }
+      ],
+      "criteria": [
+        {
+          "id": "shift-once",
+          "label": "label shift 一次",
+          "required": true,
+          "conceptId": "label-shift",
+          "text": "能用一个短例子说明输入与标签如何对齐，并且实现里只 shift 一次。"
+        },
+        {
+          "id": "overfit-small",
+          "label": "小样本过拟合",
+          "required": true,
+          "conceptId": "overfit-loop",
+          "text": "训练集 loss 明显下降；保留了不参与训练的验证样本并记录其 loss。"
+        }
+      ]
+    },
+    {
+      "id": "lab.pretrain.short-run",
+      "stageId": "lab",
+      "weekBudget": 5,
+      "shortTitle": "短程预训练",
+      "title": "跑一次可复现的短程预训练并会恢复 checkpoint",
+      "goal": "用自己的小数据和固定种子跑一段可从头复现的训练；会保存并恢复模型、optimizer、scheduler 和步数；报告验证 loss。",
+      "conceptIds": ["checkpoint", "tokenizer-bpe", "overfit-loop"],
+      "workspace": {
+        "where": "例如 ~/llm-lab/pretrain_run.py 与一个本地数据文件。不要写进六个旧作业目录。",
+        "inputs": "一份小语料（自己的 txt/jsonl 即可）；固定种子；简单学习率（常数或带 warmup 的衰减）。",
+        "outputs": "可再次运行得到相近曲线的命令记录；一个 checkpoint 文件；验证 loss 数字；简短日志。"
+      },
+      "materials": [
+        {
+          "role": "required",
+          "chapterId": "nlp",
+          "section": "6.3 BPE：字节对编码 ★",
+          "label": "6.3 BPE"
+        },
+        {
+          "role": "required",
+          "chapterId": "nlp",
+          "section": "6.4 BPE Training 与 Encoding 必须分开理解 ★",
+          "label": "6.4 训练与编码分开"
+        },
+        {
+          "role": "required",
+          "chapterId": "pretrain-sft",
+          "section": "21.7 Warmup + Learning Rate Decay ★",
+          "label": "21.7 学习率调度"
+        },
+        {
+          "role": "required",
+          "chapterId": "pytorch",
+          "section": "11.12 state_dict / checkpoint：保存与续训",
+          "label": "11.12 checkpoint"
+        },
+        {
+          "role": "required",
+          "chapterId": "pretrain-sft",
+          "section": "21.8 为什么看 Loss 还不够",
+          "label": "21.8 为什么看 Loss 还不够"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "build-llm-2",
+          "section": "Lab 9：预训练",
+          "label": "Lab 9 预训练（写完后对照流程，不要整段复制）"
+        },
+        {
+          "role": "parallel",
+          "chapterId": "evaluation",
+          "section": "3.2 数据划分",
+          "label": "3.2 数据划分"
+        }
+      ],
+      "steps": [
+        {
+          "title": "固定种子和数据切分",
+          "body": "写下如何划分训练/验证、随机种子、命令行如何启动。Tokenizer 可以用现成小词表或自己在很小语料上练 BPE；要能说明 train 与 encode 的差别。"
+        },
+        {
+          "title": "保存和恢复四样东西",
+          "body": "checkpoint 至少包含 model、optimizer、scheduler（若有）、step。中断后从该文件恢复，下一步的 step 编号接上，而不是从 0 假装续训。"
+        },
+        {
+          "title": "报告验证 loss",
+          "body": "短程即可，不求把模型训好。记录最终验证 loss。不要引入 DDP、FlashAttention 或大规模集群配置。"
+        },
+        {
+          "title": "卡住时可以分层求助",
+          "body": "先留下一次保存/加载尝试。优先问 state_dict 缺了什么。仍卡住再看 11.12。合上后重写 save/load，换一个 step 数再试。"
+        },
+        {
+          "title": "用一个小变式检查能否迁移",
+          "body": "加载 checkpoint 后再训几步，确认 step 计数增加且没有把验证集误写入训练。"
+        }
+      ],
+      "criteria": [
+        {
+          "id": "repro-run",
+          "label": "可复现短跑",
+          "required": true,
+          "conceptId": "checkpoint",
+          "text": "用记录下来的命令能从头跑完这一段；种子和数据切分写得清楚。"
+        },
+        {
+          "id": "resume-ckpt",
+          "label": "恢复训练状态",
+          "required": true,
+          "conceptId": "checkpoint",
+          "text": "能恢复模型、optimizer、scheduler（若使用）和步数，而不是只保存权重。"
+        },
+        {
+          "id": "report-val",
+          "label": "报告验证 loss",
+          "required": true,
+          "conceptId": "overfit-loop",
+          "text": "写下本次短跑的验证 loss，并说明它不是训练集数字的复制。"
+        }
+      ]
+    },
+    {
+      "id": "lab.sft.response-mask",
+      "stageId": "lab",
+      "weekBudget": 6,
+      "shortTitle": "SFT 监督范围",
+      "title": "打印逐 token 监督范围，确认 prompt 不进 loss",
+      "goal": "实现一条 SFT 样本的 chat template 与 response-only loss：prompt 和 padding 使用 ignore index（如 -100），只在助手回答上算交叉熵。",
+      "conceptIds": ["sft-mask", "label-shift", "tokenizer-bpe"],
+      "workspace": {
+        "where": "例如 ~/llm-lab/sft_mask.py。用假对话数据即可。不要去做旧 sft-lora 作业仓库。",
+        "inputs": "至少一条 {user, assistant} 样本；tokenizer（可用很小的占位分词）。",
+        "outputs": "打印每个 token 是 prompt / 回答 / padding，以及是否计入 loss；一条能讲清特殊 token 的说明。"
+      },
+      "materials": [
+        {
+          "role": "required",
+          "chapterId": "pretrain-sft",
+          "section": "21.10 Pretrain 与 SFT ★",
+          "label": "21.10 Pretrain 与 SFT"
+        },
+        {
+          "role": "required",
+          "chapterId": "pretrain-sft",
+          "section": "21.11 Chat Template",
+          "label": "21.11 Chat Template"
+        },
+        {
+          "role": "required",
+          "chapterId": "pretrain-sft",
+          "section": "21.12 SFT 的 Loss Mask ★",
+          "label": "21.12 SFT 的 Loss Mask"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "build-llm-2",
+          "section": "Lab 13：SFT（让模型学会「回答问题」）",
+          "label": "Lab 13 SFT（写完后对照，不要整段复制）"
+        },
+        {
+          "role": "reference",
+          "chapterId": "huggingface",
+          "section": "26.1 工作流地图（先建立心智模型）",
+          "label": "26.1 HF 工作流（查阅 API，不是本周作业）"
+        },
+        {
+          "role": "parallel",
+          "chapterId": "pytorch",
+          "section": "11.8 Dataset / DataLoader：喂数据",
+          "label": "11.8 组 batch 与 padding"
+        }
+      ],
+      "steps": [
+        {
+          "title": "先拼一条模板再 mask",
+          "body": "把 user/assistant 拼成模型看到的字符串，标出特殊 token。先打印 token 列表，再写 labels。"
+        },
+        {
+          "title": "只在回答上算 loss",
+          "body": "prompt 与 padding 的 label 设为 -100 或等价 ignore index。用一个 8–20 个 token 的短样本，逐个核对。"
+        },
+        {
+          "title": "卡住时可以分层求助",
+          "body": "先留下一次 mask 尝试。优先问为什么某个 token 不该进 loss。仍卡住再看 21.12 数字算例。合上后重写 labels，换一条更长的 prompt 再印一遍。"
+        },
+        {
+          "title": "用一个小变式检查能否迁移",
+          "body": "加长 padding 或插入第二轮对话，确认新的 pad 仍不计 loss，回答区间仍然计。"
+        }
+      ],
+      "criteria": [
+        {
+          "id": "print-mask",
+          "label": "打印监督范围",
+          "required": true,
+          "conceptId": "sft-mask",
+          "text": "能打印一条样本里每个 token 是否计入预期 loss。"
+        },
+        {
+          "id": "prompt-ignored",
+          "label": "prompt/padding 不计 loss",
+          "required": true,
+          "conceptId": "sft-mask",
+          "text": "确认 prompt 与 padding 不进入交叉熵；能解释用到的特殊 token。"
+        }
+      ]
+    },
+    {
+      "id": "lab.eval.simple-scorer",
+      "stageId": "lab",
+      "weekBudget": 7,
+      "shortTitle": "简化评测",
+      "title": "选一套选择题协议，手写 scorer 并分开错误与零分",
+      "goal": "在 C3 或 XCOPA 一类协议上做简化 evaluator：用手算样例检查计分；模型异常与真实零分分开记；记下 prompt、选项计分方式和模型版本。不要去做旧 llm-eval 作业仓库。",
+      "conceptIds": ["eval-scorer"],
+      "workspace": {
+        "where": "例如 ~/llm-lab/tiny_eval.py。用几条手工样例即可，不必下载全量榜。",
+        "inputs": "冻结的几条题目（题干、选项、标准答案）；模型输出或你写的假输出。",
+        "outputs": "逐样本记录：题目 id、模型输出、得分、是否异常；与手算一致的 scorer。"
+      },
+      "materials": [
+        {
+          "role": "required",
+          "chapterId": "llm-eval",
+          "section": "23.1 Evaluation Pipeline：评估也是一条流水线",
+          "label": "23.1 评估流水线"
+        },
+        {
+          "role": "required",
+          "chapterId": "llm-eval",
+          "section": "23.5 选择题准确率与随机基线",
+          "label": "23.5 选择题与随机基线"
+        },
+        {
+          "role": "required",
+          "chapterId": "llm-eval",
+          "section": "23.3 Exact Match：解析器决定分数",
+          "label": "23.3 解析器决定分数"
+        },
+        {
+          "role": "required",
+          "chapterId": "llm-eval",
+          "section": "23.17 Failure Case Analysis：分数之外",
+          "label": "23.17 失败案例分析"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "build-llm-2",
+          "section": "Lab 10：评估",
+          "label": "Lab 10 评估（对照流程，不是旧作业仓库）"
+        },
+        {
+          "role": "parallel",
+          "chapterId": "evaluation",
+          "section": "3.4 Accuracy、Precision、Recall、F1",
+          "label": "3.4 分类指标（查阅）"
+        }
+      ],
+      "steps": [
+        {
+          "title": "先手算再写 scorer",
+          "body": "选 3 条题：一条应得满分、一条应得 0、一条输出格式坏掉。先在纸上给出分数，再写函数。"
+        },
+        {
+          "title": "异常与零分分开",
+          "body": "解析失败、超时、空输出记为异常，不要直接当成答错的 0 分混进准确率分母时不加说明。准确率分母写清楚。"
+        },
+        {
+          "title": "记下协议",
+          "body": "记录 prompt 模板、选项如何计分、模型名称与版本。不把旧 llm-eval 项目当作本周交付物。"
+        },
+        {
+          "title": "卡住时可以分层求助",
+          "body": "先留下 scorer 草稿。优先问某条坏输出该进哪一类。仍卡住再看 23.3。合上后重写解析分支，再加一条新的坏输出。"
+        },
+        {
+          "title": "用一个小变式检查能否迁移",
+          "body": "把正确答案的表面字符串改成大小写或前后空格不同，看你的解析是否仍稳定；若不稳定，写明限制。"
+        }
+      ],
+      "criteria": [
+        {
+          "id": "hand-scorer",
+          "label": "手算核对 scorer",
+          "required": true,
+          "conceptId": "eval-scorer",
+          "text": "至少三条样例上，函数输出与手算一致。"
+        },
+        {
+          "id": "error-vs-zero",
+          "label": "异常与零分分开",
+          "required": true,
+          "conceptId": "eval-scorer",
+          "text": "格式失败等异常不与真正答错的零分混成一类而不加说明。"
+        },
+        {
+          "id": "protocol-note",
+          "label": "记录协议与版本",
+          "required": true,
+          "conceptId": "eval-scorer",
+          "text": "写下 prompt、选项计分方式和模型版本。"
+        }
+      ]
+    },
+    {
+      "id": "lab.experiment.one-variable",
+      "stageId": "lab",
+      "weekBudget": 8,
+      "shortTitle": "单变量实验",
+      "title": "只改一个变量，用同一套评测比较并解释局限",
+      "goal": "在已有小模型上做一次控制变量实验：只改数据量或学习率等一个因素，用第 7 周同一套评测比较。没有提升也如实记录。缓存加速是拓展，不是硬门槛。",
+      "conceptIds": ["one-variable", "kv-cache", "eval-scorer"],
+      "workspace": {
+        "where": "例如 ~/llm-lab/experiment.md 加两次训练命令。不要新开旧 inference-benchmark 作业。",
+        "inputs": "基线配置 A；只改一个旋钮的配置 B；同一评测脚本与同一测试题。",
+        "outputs": "结果表、种子、bad case、局限说明；可选：有/无 KV cache 的生成结果对照（不要求速度数字）。"
+      },
+      "materials": [
+        {
+          "role": "required",
+          "chapterId": "pretrain-sft",
+          "section": "21.8 为什么看 Loss 还不够",
+          "label": "21.8 为什么看 Loss 还不够"
+        },
+        {
+          "role": "required",
+          "chapterId": "llm-eval",
+          "section": "23.17 Failure Case Analysis：分数之外",
+          "label": "23.17 失败案例"
+        },
+        {
+          "role": "required",
+          "chapterId": "evaluation",
+          "section": "3.2 数据划分",
+          "label": "3.2 数据划分（测试集不要用来调参）"
+        },
+        {
+          "role": "check-after",
+          "chapterId": "gpt",
+          "section": "9.4 推理循环：自回归生成",
+          "label": "9.4 生成循环（输出对照）"
+        },
+        {
+          "role": "reference",
+          "chapterId": "modern-llm",
+          "section": "10.1 KV Cache：推理加速的关键 ★",
+          "label": "10.1 KV Cache（拓展：输出对照即可）"
+        },
+        {
+          "role": "reference",
+          "chapterId": "build-llm-2",
+          "section": "Lab 12：KV Cache（把生成提速一个数量级）",
+          "label": "Lab 12 KV Cache（拓展，速度分析可留到后面）"
+        }
+      ],
+      "steps": [
+        {
+          "title": "先写清只改什么",
+          "body": "一句话写出自变量、不变量、评测集。种子写进表。不要同时改结构、数据和学习率。"
+        },
+        {
+          "title": "跑完两次并用同一评测",
+          "body": "A/B 用第 7 周 scorer。没有提升也留下数字和 1–2 条 bad case。解释可能的局限（数据太小、评测太窄）。"
+        },
+        {
+          "title": "卡住时可以分层求助",
+          "body": "先留下实验记录草稿。优先问两个配置是否其实改了两处。合上参考后重写对照表。"
+        },
+        {
+          "title": "用一个小变式检查能否迁移",
+          "body": "把同一结论用另一条 bad case 复述一遍：失败是模型不会，还是评测题本身含糊。"
+        }
+      ],
+      "criteria": [
+        {
+          "id": "one-knob",
+          "label": "只改一个变量",
+          "required": true,
+          "conceptId": "one-variable",
+          "text": "实验记录里能看出只改了数据量或学习率等一个因素，其它保持不变。"
+        },
+        {
+          "id": "same-eval",
+          "label": "同一评测比较",
+          "required": true,
+          "conceptId": "eval-scorer",
+          "text": "用同一套题目和 scorer 比较；没有提升也如实记录并解释局限。"
+        },
+        {
+          "id": "cache-speed",
+          "label": "缓存速度分析",
+          "required": false,
+          "conceptId": "kv-cache",
+          "text": "拓展：有/无 KV cache 的生成输出对照。速度数字不是本周硬门槛。"
+        }
+      ]
     }
   ]
 }
diff --git a/css/course.css b/css/course.css
index 8e5a5fa..288ff4a 100644
--- a/css/course.css
+++ b/css/course.css
@@ -22,7 +22,21 @@
 .resume-panel, .task-panel { padding: var(--space-8); background: var(--bg-panel); border: 1px solid var(--border); border-radius: var(--radius); }
 .resume-panel h2, .task-panel h2 { margin-top: var(--space-2); }
 .resume-panel p, .task-panel p { color: var(--text-soft); max-width: 38em; margin: var(--space-2) 0 var(--space-6); }
-.task-panel .btn, .resume-panel .btn { margin-top: var(--space-2); }
+.week-picker { display: flex; flex-wrap: wrap; gap: var(--space-2); margin: var(--space-4) 0 var(--space-2); }
+.week-pick {
+  min-height: 44px;
+  padding: 0 var(--space-3);
+  border: 1px solid var(--border);
+  background: var(--bg-soft);
+  color: var(--text);
+  border-radius: var(--radius);
+  font-size: var(--text-sm);
+}
+.week-pick[aria-pressed="true"] {
+  border-color: var(--accent);
+  background: var(--accent-soft);
+  color: var(--accent-text);
+}
 .task-list, .concept-list { margin: 0; padding-left: 1.25em; max-width: 46em; }
 .task-list li, .concept-list li { margin: 0 0 var(--space-3); }
 .task-list a, .concept-list a { color: var(--accent-text); min-height: 44px; display: inline-flex; align-items: center; }
diff --git a/docs/IMPLEMENTATION_PLAN.md b/docs/IMPLEMENTATION_PLAN.md
index 097c559..115becc 100644
--- a/docs/IMPLEMENTATION_PLAN.md
+++ b/docs/IMPLEMENTATION_PLAN.md
@@ -105,7 +105,7 @@
 
 **进入本阶段前：** [issue #1](https://github.com/xhr0417/llm-course/issues/1) 的 7.6 对称性口径与 README Pages 入口；[issue #2](https://github.com/xhr0417/llm-course/issues/2) 不再把共享 Q/K 写成无法计算；[issue #3](https://github.com/xhr0417/llm-course/issues/3) 的学习设计（验收／失败、求助、必需／拓展、八周难度）。未完成这些不要开始实现本阶段功能。
 
-**本轮（issue #3）只同步计划设计：** 更新 OS、一年计划、本文件，以及现有 Attention 任务文案与 `required` 字段。`learning-plan.json` 仍只有第 1 周任务；**不实现**记录表单、八周 JSON 扩写、任务上下文导航。创建本 issue 不等于阶段 3 已实施。
+**本轮（阶段 3 第一步）：** 前八周任务写入 `learning-plan.json`（每条含写什么、怎么检查、必需／拓展、真实教材小节），首页可手动选择当前周。`evidenceSystemImplemented` 仍为 `false`。**不实现**记录表单、卡点存储、任务上下文导航。第 0／24 章遗留修改仍单独处理。
 
 **开工后的实现顺序（可验收小步，不扩大功能）：**
 
diff --git a/js/app.js b/js/app.js
index cc8fe0b..d4c42eb 100644
--- a/js/app.js
+++ b/js/app.js
@@ -7,6 +7,7 @@
   var escape = renderer.escapeHtml;
   var chapters = [], tracks = [], references = [], current = null, currentPath = null, activeTrack = null, pages, search;
   var cache = {}, requestId = 0, pendingSection = null;
+  var learningPlan = null, planError = null, selectedTaskId = stored("llm-course-current-task");
   var reader = CourseReader.create({ root: root, tocNav: byId("tocNav"), escapeHtml: escape });
   function stored(key) { try { return localStorage.getItem(key); } catch (error) { return null; } }
   function remember(key, value) { try { localStorage.setItem(key, value); } catch (error) { /* Session remains usable without persistence. */ } }
@@ -99,6 +100,19 @@
       entry(pages.home(readLast()), "我的学习");
       var retryPlan = byId("retryPlan");
       if (retryPlan) retryPlan.addEventListener("click", loadLearningPlan);
+      root.querySelectorAll("[data-week]").forEach(function (button) {
+        button.addEventListener("click", function () {
+          if (!learningPlan) return;
+          var week = Number(button.getAttribute("data-week"));
+          var task = learningPlan.tasks.find(function (item) { return item.weekBudget === week; });
+          if (!task) return;
+          selectedTaskId = task.id;
+          remember("llm-course-current-task", task.id);
+          pages = CoursePages({ chapters: chapters, tracks: tracks, plan: learningPlan, planError: planError,
+            selectedTaskId: selectedTaskId, references: references, progress: progress, escapeHtml: escape });
+          route();
+        });
+      });
       return;
     }
     if (path === "catalog") return entry(pages.catalog(), "全部章节");
@@ -189,8 +203,13 @@
     search.start();
   }
   function applyPlan(plan, error) {
+    learningPlan = plan;
+    planError = error;
+    if (plan && selectedTaskId && !plan.tasks.some(function (item) { return item.id === selectedTaskId; })) {
+      selectedTaskId = null;
+    }
     pages = CoursePages({ chapters: chapters, tracks: tracks, plan: plan, planError: error,
-      references: references, progress: progress, escapeHtml: escape });
+      selectedTaskId: selectedTaskId, references: references, progress: progress, escapeHtml: escape });
     startRoutingAndSearch();
     if (currentPath === "home") route();
   }
diff --git a/js/pages.js b/js/pages.js
index 2d32b4c..340ba99 100644
--- a/js/pages.js
+++ b/js/pages.js
@@ -36,6 +36,25 @@
     function stretchCriteria(task) {
       return (task.criteria || []).filter(function (item) { return item.required === false; });
     }
+    function resolveTask(plan) {
+      var selected = options.selectedTaskId;
+      var match = selected && plan.tasks.find(function (item) { return item.id === selected; });
+      return match || plan.tasks.find(function (item) { return item.id === plan.currentTaskId; });
+    }
+    function weekButtons(plan, stage, task) {
+      var weeks = plan.tasks.filter(function (item) { return item.stageId === stage.id; })
+        .slice()
+        .sort(function (a, b) { return (a.weekBudget || 0) - (b.weekBudget || 0); });
+      if (weeks.length < 2) return "";
+      return '<div class="week-picker" data-week-picker role="group" aria-label="选择当前练习">' +
+        weeks.map(function (item) {
+          var on = item.id === task.id;
+          return '<button type="button" class="week-pick" data-week="' + String(item.weekBudget) +
+            '" aria-pressed="' + on + '">第 ' + String(item.weekBudget) + " 周 · " +
+            escape(item.shortTitle || item.title) + "</button>";
+        }).join("") +
+        '</div><p class="page-note">点某一周只更换练习说明。不会把前面的周标成完成，也不会按日期自动跳周。</p>';
+    }
     function home(last) {
       if (options.planError) {
         return header("我的学习", "当前练习暂时无法显示，教材仍可查阅。") +
@@ -45,7 +64,7 @@
           '<p class="page-note">目录、章节和搜索仍然可用。也可以先浏览<a href="#/catalog">全部章节</a>。</p>';
       }
       var stage = plan && plan.stages.find(function (item) { return item.id === plan.currentStageId; });
-      var task = plan && plan.tasks.find(function (item) { return item.id === plan.currentTaskId; });
+      var task = plan && resolveTask(plan);
       var conceptsById = {};
       (plan && plan.concepts || []).forEach(function (item) { conceptsById[item.id] = item; });
       if (!stage || !task) {
@@ -68,11 +87,12 @@
       var resume = previous ? '<section class="resume-panel"><span class="page-label">上次打开的教材</span><h2>' +
         escape(name(previous)) + '</h2><p>这是阅读位置，不是练习完成，也不表示已掌握。</p>' +
         action("继续阅读", routes.href(previous.id, lastTrack)) + '</section>' : "";
-      return header("小模型学习实验室—Attention",
-        "当前练习：在空文件里写出因果多头注意力，并检查形状、因果性和数值。",
+      return header(stage.title + "—" + (task.shortTitle || task.title),
+        "当前练习：" + task.title + "。先看写什么、怎么检查、必需做到哪里。",
         "我的学习") +
         '<section class="task-panel"><span class="page-label">当前练习</span><h2>' + escape(task.title) + '</h2>' +
         '<p>' + escape(task.goal) + '</p>' +
+        weekButtons(plan, stage, task) +
         (start ? action("打开必要教材：" + start.label, materialHref(start)) : "") +
         '</section>' +
         '<section class="entry-section"><h2>必要教材</h2><ol class="task-list">' +
@@ -86,9 +106,11 @@
         task.steps.map(function (step) {
           return "<li><strong>" + escape(step.title) + "</strong><p>" + escape(step.body) + "</p></li>";
         }).join("") + "</ol></section>" +
-        '<section class="entry-section"><h2>如何检查结果</h2><ol class="task-list">' +
+        '<section class="entry-section"><h2>如何检查结果</h2>' +
+        (after.length ? '<ol class="task-list">' +
         after.map(function (item) { return "<li>" + materialLink(item) + "</li>"; }).join("") +
-        '</ol><p class="page-note">本页不代写核心算法，也不在浏览器里运行 Python。先留下一次自己的实现尝试再检查；卡住时可以分层求助，看过参考后重写关键部分并换输入验证。</p></section>' +
+        "</ol>" : "") +
+        '<p class="page-note">本页不代写核心算法，也不在浏览器里运行 Python。先留下一次自己的实现尝试再检查；卡住时可以分层求助，看过参考后重写关键部分并换输入验证。</p></section>' +
         '<section class="entry-section"><h2>验收条件</h2><ol class="task-list">' +
         requiredCriteria(task).map(function (item) {
           return "<li><strong>" + escape(item.label || "验收") + "</strong><p>" + escape(item.text) + "</p></li>";
@@ -104,9 +126,9 @@
           var concept = conceptsById[id];
           return "<li>" + escape(concept ? concept.name : "未命名概念") + "</li>";
         }).join("") + "</ul></section>" +
-        '<section class="entry-section"><h2>并行补基础</h2><ul class="task-list">' +
+        (parallel.length ? '<section class="entry-section"><h2>并行补基础</h2><ul class="task-list">' +
         parallel.map(function (item) { return "<li>" + materialLink(item) + "</li>"; }).join("") +
-        '</ul><p class="page-note">卡在 class、reshape 或 broadcasting 时，打开对应小节即可。</p></section>' +
+        '</ul><p class="page-note">卡在 class、reshape 或 broadcasting 时，打开对应小节即可。</p></section>' : "") +
         (reference.length ? '<section class="entry-section"><h2>对照入口</h2><ul class="task-list">' +
           reference.map(function (item) { return "<li>" + materialLink(item) + "</li>"; }).join("") +
           "</ul></section>" : "") +
@@ -136,7 +158,7 @@
     }
     function projectPage() {
       return header("已退出主线", "六个旧作业项目不再作为主要学习入口。目录仍留在仓库中，待后续审计后再处理。", "说明") +
-        '<section class="task-panel"><p>当前主线从<a href="#/">我的学习</a>进入，任务是小模型学习实验室—Attention。旧书签打开本页不会失效。</p>' +
+        '<section class="task-panel"><p>当前主线从<a href="#/">我的学习</a>进入。旧书签打开本页不会失效。</p>' +
         '<p>理论教材、交互演示和搜索仍在。需要查阅章节时打开<a href="#/catalog">全部章节</a>。</p>' +
         action("回到我的学习", "#/") + '</section>' +
         '<p class="page-note">第 25–31 章仍可当教材阅读。其中的历史仓库链接已标明可选参考，不是当前作业。</p>';
diff --git a/tools/test-app-harness.js b/tools/test-app-harness.js
index 13d73fb..d00e2aa 100644
--- a/tools/test-app-harness.js
+++ b/tools/test-app-harness.js
@@ -390,7 +390,7 @@ function bootApp(options) {
   Object.keys(byId).forEach(function (id) { byId[id].ownerDocument = null; });
 
   const location = { hash: options.hash || "#/" };
-  const storage = Object.create(null);
+  const storage = Object.assign(Object.create(null), options.storage || {});
   const localStorage = {
     getItem: function (key) { return Object.prototype.hasOwnProperty.call(storage, key) ? storage[key] : null; },
     setItem: function (key, value) { storage[key] = String(value); },
diff --git a/tools/test-course.js b/tools/test-course.js
index b9f5f9f..056586c 100644
--- a/tools/test-course.js
+++ b/tools/test-course.js
@@ -286,9 +286,46 @@ test('learning plan current task is executable Attention and points at real head
     assert.ok(chapter, item.chapterId);
     assert.ok(headingsFor(chapter.file).includes(item.section), `${item.chapterId} 缺少小节 ${item.section}`);
   });
+  assert.equal(plan.tasks.length, 8);
+  assert.equal(new Set(plan.tasks.map(item => item.id)).size, 8);
+  const weeks = plan.tasks.map(item => item.weekBudget).sort((a, b) => a - b);
+  assert.deepEqual(weeks, [1, 2, 3, 4, 5, 6, 7, 8]);
+  const conceptIds = new Set(plan.concepts.map(item => item.id));
+  plan.tasks.forEach(item => {
+    assert.ok(item.workspace && item.workspace.where && item.workspace.inputs && item.workspace.outputs, item.id);
+    assert.match(item.workspace.where, /空文件|本机|~\//);
+    assert.doesNotMatch(item.workspace.where, /projects\/(?:log-analyzer|hf-mini-lab|llm-eval|rag-service|sft-lora|inference-benchmark)/);
+    assert.ok(item.steps && item.steps.length >= 3, item.id);
+    assert.ok(item.steps.some(step => /求助/.test(step.title)), item.id);
+    assert.ok(item.criteria.some(row => row.required !== false), item.id);
+    const ids = item.criteria.map(row => row.id);
+    assert.equal(new Set(ids).size, ids.length, item.id);
+    const requiredText = item.criteria.filter(row => row.required !== false).map(row => row.text).join('\n');
+    assert.doesNotMatch(requiredText, /DDP|FlashAttention|GRPO/);
+    item.conceptIds.forEach(id => assert.ok(conceptIds.has(id), `${item.id} 未知概念 ${id}`));
+    item.criteria.forEach(row => {
+      if (row.conceptId) assert.ok(conceptIds.has(row.conceptId), `${item.id} ${row.id}`);
+    });
+    (item.materials || []).forEach(material => {
+      if (material.href) return;
+      const chapter = byId[material.chapterId];
+      assert.ok(chapter, `${item.id} ${material.chapterId}`);
+      assert.ok(headingsFor(chapter.file).includes(material.section), `${item.id} ${material.chapterId} 缺少小节 ${material.section}`);
+    });
+  });
+  const week3 = plan.tasks.find(item => item.weekBudget === 3);
+  const gqa = week3.criteria.find(item => item.id === 'gqa-heads');
+  assert.equal(gqa.required, false);
+  assert.match(gqa.text, /不能阻挡|不阻挡/);
+  const week2 = plan.tasks.find(item => item.weekBudget === 2);
+  assert.match(week2.workspace.outputs, /\[B, T, V\]/);
+  assert.equal(week2.criteria.find(item => item.id === 'norm-rope-read').required, false);
+  const week8 = plan.tasks.find(item => item.weekBudget === 8);
+  assert.equal(week8.criteria.find(item => item.id === 'cache-speed').required, false);
 });
 
-function loadPages(plan, planError) {
+function loadPages(plan, planError, extra) {
+  extra = extra || {};
   const vm = require('node:vm');
   const pagesSource = fs.readFileSync(path.resolve(__dirname, '..', 'js/pages.js'), 'utf8');
   const sandbox = { window: { CourseRoutes: routes } };
@@ -298,6 +335,7 @@ function loadPages(plan, planError) {
     tracks,
     plan,
     planError,
+    selectedTaskId: extra.selectedTaskId,
     references,
     progress: { isRead: () => false },
     escapeHtml: value => String(value)
@@ -324,6 +362,8 @@ test('primary entry shows the Attention lab and retires homework projects from m
   assert.match(home, /无掩码/);
   assert.match(home, /#\/transformer\?section=/);
   assert.doesNotMatch(home, /7\.12[\s\S]{0,80}1e-5|1e-5[\s\S]{0,80}7\.12/);
+  assert.match(home, /第 8 周/);
+  assert.match(home, /data-week="2"/);
   assert.doesNotMatch(home, /自动测试通过/);
   assert.match(home, /分层求助/);
   assert.match(home, /小变式/);
@@ -338,6 +378,24 @@ test('primary entry shows the Attention lab and retires homework projects from m
   assert.doesNotMatch(retired, /N 个可运行项目|个可运行项目/);
 });
 
+test('home can show another week without treating that as earlier completion', () => {
+  const plan = require('../content/learning-plan.json');
+  const week2 = loadPages(plan, null, { selectedTaskId: 'lab.decoder.min-lm' }).home(null);
+  assert.match(week2, /拼最小 decoder/);
+  assert.match(week2, /\[B, T, V\]/);
+  assert.match(week2, /拓展（不挡完成）/);
+  assert.match(week2, /data-week="2"[^>]*aria-pressed="true"|aria-pressed="true"[^>]*data-week="2"/);
+  assert.doesNotMatch(week2, /手写因果多头注意力/);
+  assert.doesNotMatch(week2, /lab\.decoder|criterionId|<code>/);
+  assert.match(week2, /不会把前面的周标成完成/);
+  const week3 = loadPages(plan, null, { selectedTaskId: 'lab.train.step-update' }).home(null);
+  assert.match(week3, /一次参数更新/);
+  assert.match(week3, /GQA/);
+  assert.match(week3, /不能阻挡|不挡完成/);
+  assert.match(week3, /拓展（不挡完成）/);
+  assert.doesNotMatch(week3, /自动测试通过/);
+});
+
 test('phase 3 design docs agree on completion, help, and GQA stretch without claiming implementation', () => {
   const os = fs.readFileSync(path.resolve(__dirname, '..', 'docs/PERSONAL_LEARNING_OS.md'), 'utf8');
   const year = fs.readFileSync(path.resolve(__dirname, '..', 'docs/LEARNING_PLAN.md'), 'utf8');
@@ -354,9 +412,9 @@ test('phase 3 design docs agree on completion, help, and GQA stretch without cla
   assert.match(year, /GQA 是拓展项/);
   assert.match(eightWeeks, /\*\*不能\*\*用这项阻挡本周核心任务完成/);
   assert.match(impl, /issue #3/);
-  assert.match(impl, /只同步计划设计/);
+  assert.match(impl, /前八周任务写入/);
   assert.match(impl, /GQA 为拓展项/);
-  assert.match(impl, /evidenceSystemImplemented|不实现/);
+  assert.match(impl, /evidenceSystemImplemented.*false|仍为 `false`/);
   assert.doesNotMatch(impl, /本轮实现阶段 3 功能/);
 });
 
@@ -496,3 +554,29 @@ test('late plan success or failure does not rerender an open chapter', async ()
   await openChapterDuringPlan(false);
   await openChapterDuringPlan(true);
 });
+
+test('home week picker switches the current exercise and remembers it', async () => {
+  const app = harness.bootApp({ hash: '#/' });
+  await harness.waitFor(() => /小模型学习实验室—Attention/.test(app.text()));
+  const week2 = app.content.querySelector('[data-week="2"]');
+  assert.ok(week2);
+  week2.click();
+  await harness.waitFor(() => /拼最小 decoder/.test(app.text()));
+  assert.match(app.text(), /\[B, T, V\]/);
+  assert.doesNotMatch(app.text(), /手写因果多头注意力/);
+  assert.equal(app.content.querySelector('[data-week="2"]').getAttribute('aria-pressed'), 'true');
+  assert.equal(app.localStorage.getItem('llm-course-current-task'), 'lab.decoder.min-lm');
+
+  const week3 = app.content.querySelector('[data-week="3"]');
+  week3.click();
+  await harness.waitFor(() => /一次参数更新/.test(app.text()));
+  assert.match(app.text(), /拓展（不挡完成）/);
+  assert.match(app.text(), /GQA/);
+
+  const restored = harness.bootApp({
+    hash: '#/',
+    storage: { 'llm-course-current-task': 'lab.decoder.min-lm' }
+  });
+  await harness.waitFor(() => /拼最小 decoder/.test(restored.text()));
+  assert.doesNotMatch(restored.text(), /手写因果多头注意力/);
+});
